import 'package:flutter/material.dart';

class WalktroughColors {
  static const Color walktroughbackgroundColor = Colors.white;
  static Color walktroughTextColor = Color.fromRGBO(128, 130, 131, 1);
  static const Color pcIconButtonColor = Colors.grey;
}

class PageColors {
  static const Color pageBackgroundColor = Colors.white;
  static Color pageTextColor = Color.fromRGBO(128, 130, 131, 1);
  static Color buttonColor = Colors.grey.shade700;
  static const Color buttonTextColor = Colors.white;
  static Color appBarColor = Colors.grey.shade800;
  static const Color appBarTextColor = Colors.white;
  static const Color appBarButtonColor = Colors.white;
  static Color textFormFieldHintColor = Colors.black87; // !!!!!
  static const Color textFormFieldBorderColor = Colors.black;
}
